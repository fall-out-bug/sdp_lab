# WS-002-02: AgentRun RBAC, kustomize manifests, and deployment

Feature: FR-002 (sdp_dev-kel)
Priority: P0
Effort: 1d
Dependencies: WS-002-01

## Goal

Package AgentRun controller for deployment on minikube cluster.

## Scope

1. ServiceAccount, Role, RoleBinding for adapter-controller
2. Deployment manifest for adapter-controller
3. CRD manifest (from controller-gen output)
4. Kustomization.yaml for deploy/k8s/adapter/
5. Dockerfile for adapter-controller

## Acceptance Criteria

- [x] `kubectl kustomize deploy/k8s/adapter/` validates
- [x] Deployment uses adapter-controller image
- [x] RBAC allows: get/list/watch/create/update AgentRun, Task
- [x] RBAC allows: get/list pods (for log collection)
- [x] CRD installed via kustomize

## Execution Report (2026-02-22)

- deploy/k8s/adapter/base/: deployment, rbac, namespace; overlays/dev, staging, prod
- RBAC: agentruns, tasks (Agent from kubeopencode if needed)
