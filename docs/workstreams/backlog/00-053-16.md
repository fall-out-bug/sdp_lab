---
ws_id: 00-053-16
feature_id: F053
status: done
priority: P0
size: M
depends_on: []
---

# 00-053-16: Pipeline Hooks Shell Injection — Replace sh -c

Feature: F053 (sdp_dev-8wzg)  
Bead: sdp_dev-0ddg (review finding)

## Goal

Replace `exec.CommandContext(..., "sh", "-c", h.Command)` in pipeline hooks with allowlist of executables + args as list, or restricted DSL. Malicious PR can add hooks that run arbitrary code in CI.

## Scope Files

- `internal/orchestrate/hooks.go`
- `.sdp/pipeline-hooks.yaml.example`
- `docs/drafts/idea-phase4-remediation-round2.md`

## Dependencies

None.

## Acceptance Criteria

- [x] Replace `sh -c` with allowlist of executables; pass args as list
- [x] `.sdp/pipeline-hooks.yaml` schema updated: `command` → `executable` + `args` array
- [x] Backward compatibility: if `command` present, reject or migrate (document)
- [x] go build ./... passes
- [x] go test ./internal/orchestrate/... passes

## Execution Report

**Commit:** c570ac6  
**Verdict:** PASS  
**Date:** 2026-02-25

**Summary:** Replaced `sh -c` with `executable` + `args` array. Added allowlist validation (echo, go, make, python3, node, npm, npx, bash, sh, false). Legacy `command` field rejected at load. Reject `sh -c` / `bash -c` to prevent shell injection. Updated `.sdp/pipeline-hooks.yaml.example` and idea doc with migration note.

## Beads

- sdplab-8wzg: legacy reference (done)
