---
ws_id: 00-153-02
feature_id: F153
status: open
priority: P2
size: S
depends_on:
  - 00-153-01
---

# 00-153-02: Naming policy

Feature: F153 (sdplab-mbhg)
Parent bead: sdplab-mbhg.2

## Goal

Write the canonical naming policy so future agents and engineers apply prefix / namespace rules consistently without asking each time.

## Beads

- sdplab-mbhg.2

## Scope Files

- `docs/strategy/naming-policy.md` — naming policy document
- `AGENTS.md` — add pointer to naming policy in §Constraints (if policy is stable)

## Acceptance Criteria

- [ ] Rule: when `sdp-` prefix is required (Toolkit binaries, Toolbox tools in SDP namespace)
- [ ] Rule: when `sdp-` prefix is forbidden (IIP tools with `standalone: true` — no `sdp-` from inception)
- [ ] Display-vs-internal namespace decoupling rule (display name can differ from package path)
- [ ] Working-name rename criteria formalized: domain available, no trademark collision, ICP recognizes, council/buyer language test passes
- [ ] Toolbox tool naming rule: what prefix, how versioned, when they graduate to own namespace
- [ ] IIP naming rule: neutral namespace from day one OR accept contamination (after F158 decision)
- [ ] AGENTS.md pointer added if policy is stable enough for agent consumption

## Out of Scope

- Trademark/domain checks for specific names (00-153-03)
- Brand family map (00-153-01)

## Source Material

- Memo council synthesis §2 (lock internal namespace `sdp-pr-gate`)
- IIP-council synthesis §C (manual dependency audit interim)
- SDP product layering memo v3 §IIP Flag Inside Toolbox
