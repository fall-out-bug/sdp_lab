# WS-003-03: Run controller-gen for DeepCopy and CRD manifests

Feature: FR-003 (sdp_dev-j25)
Priority: P0
Effort: 0.5d
Dependencies: WS-003-02

## Goal

Generate zz_generated.deepcopy.go and CRD YAML manifests from kubebuilder-annotated types.

## Acceptance Criteria

- [x] `make generate` target exists in Makefile
- [x] `controller-gen object paths=./api/...` generates DeepCopy
- [x] `controller-gen crd paths=./api/... output:crd:dir=deploy/k8s/crd/` generates CRD YAML
- [x] Generated CRD can be applied: `kubectl apply -f deploy/k8s/crd/`
- [x] `go build ./...` passes with generated code

## Execution Report (2026-02-22)

- Makefile: generate target with controller-gen object + crd
- deploy/k8s/crd/: _agentruns.yaml, _tasks.yaml
