# WS-001-01: Replace adapter-controller placeholder with controller-runtime Manager

Feature: FR-001 (sdp_dev-23j)
Priority: P0
Effort: 1d
Dependencies: WS-003-03 (CRD types generated)

## Goal

Replace the 82-line demo in cmd/adapter-controller/main.go with a controller-runtime Manager that starts an informer and reconcile loop.

## Acceptance Criteria

- [x] Manager starts with in-cluster or kubeconfig
- [x] Registers TaskReconciler
- [x] Connects to NATS for status events
- [x] Graceful shutdown on SIGTERM
- [x] envtest test: Manager starts and stops cleanly

## Execution Report (2026-02-22)

- cmd/adapter-controller/main.go: Manager, getKubeConfig (KUBECONFIG/home/.kube/config/in-cluster), TaskReconciler, AgentRunReconciler, NATS when NATS_URL set, signal.NotifyContext(SIGTERM,SIGINT), HealthProbeBindAddress :8081
- cmd/adapter-controller/main_test.go: TestManagerStartsAndStops (envtest; requires setup-envtest for full run)

## Steps

1. Replace main.go with controller-runtime Manager setup
2. Register scheme (api/v1alpha1 + kubeopencode shim)
3. Wire NATS connection from bus package
4. Add health/readiness probes
