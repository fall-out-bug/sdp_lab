---
ws_id: 00-053-39
feature_id: F053
status: done
priority: P2
size: M
depends_on: []
---

# 00-053-39: Root Directory Binary Files Cleanup

Feature: F053 (sdp_dev-j74i)  
Bead: sdp_dev-rfh4 (review finding)

## Goal

Remove or relocate 20+ binary/coverage files from project root. Root should not contain build artifacts, coverage.out, or orphaned binaries. Add to .gitignore if generated; delete if stale.

## Scope Files

- `.gitignore`
- Project root (ls -la)
- `Makefile` or CI scripts that write to root

## Dependencies

None.

## Acceptance Criteria

- [x] Identify all binary/coverage files in root (coverage.out, *.out, sdp-*, etc.)
- [x] Add coverage.out, *.out, sdp-evidence, sdp-orchestrate, sdp-ci-loop to .gitignore if built locally
- [x] Remove or relocate orphaned binaries; document in Makefile/README where build outputs go
- [x] `ls -la` at root shows no unexpected binaries (or they are gitignored)
- [x] go build ./... passes

---

## Execution Report

**Completed:** 2026-02-25

**Deliverables:**
- Identified 19 binaries + 24 coverage *.out files in root (all gitignored)
- .gitignore: coverage.out, *.out, sdp-* binaries already present; clarified comments
- Makefile: coverage now writes to .tmp/coverage.out; added clean-root target
- Ran make clean-root — removed orphaned binaries and coverage from root
- go build ./... passes

**Existing work summary:** .gitignore already had *.out and binary patterns. Makefile wrote coverage to root; updated to .tmp/. Added clean-root for one-time and repeat cleanup.
