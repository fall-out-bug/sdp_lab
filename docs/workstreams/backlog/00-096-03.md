---
ws_id: 00-096-03
feature_id: F096
status: backlog
priority: P2
size: M
depends_on: ["00-096-02"]
---

# 00-096-03: Shared Dolt remote parking and cutover prep

Feature: F096 (Legacy Drift Cleanup)

## Goal

Park Beads shared-Dolt provisioning as explicit tech debt while preserving a clean cutover path from git-backed backup transport to first-class Dolt push/pull.

## Beads

- sdplab-cc4c

## Blocker Resolution

Status changed from `blocked` to `backlog` on 2026-04-26:
- **00-096-02** (historical workstream triage) completed
- Repo-side bootstrap is done (`scripts/beads_dolt_remote_bootstrap.sh` exists)
- Remaining blocker: infra provisioning of shared Dolt remote

## Scope Files

- `scripts/beads_transport.sh`
- `scripts/beads_dolt_remote_bootstrap.sh`
- `docs/runbooks/beads-transport.md`
- `docs/workstreams/backlog/00-096-03.md`
- `docs/workstreams/INDEX.md`
- `docs/roadmap/ROADMAP.md`
- `.beads-sdp-mapping.jsonl`

## Acceptance Criteria

- [ ] A real Dolt remote URL is provisioned for `sdplab`
- [ ] `DOLT_REMOTE_USER` and `DOLT_REMOTE_PASSWORD` are available in the execution environment that owns Beads replication
- [ ] `scripts/beads_dolt_remote_bootstrap.sh --url <remote> --push` succeeds against the shared remote
- [ ] `./scripts/beads_transport.sh status` reports `mode=dolt-remote`
- [ ] Any remaining `SQL only` bootstrap warning is either eliminated or explicitly documented as upstream behavior

## Reference

- [2026-04-01-dolt-remote-parking-plan.md](../../archive/plans/2026-04-01-dolt-remote-parking-plan.md)
- [beads-transport.md](../../runbooks/beads-transport.md)

## Implementation Notes

### Current State

- **Transport Mode**: Git-backup (`mode=git-backup archival_only=true`)
- **Bootstrap Script**: `scripts/beads_dolt_remote_bootstrap.sh` - ready to use
- **Dolt Integration**: Beads supports Dolt remote operations
- **Status Command**: `./scripts/beads_transport.sh status` shows current mode

### Remaining Work

1. **Infra Provisioning** (external blocker):
   - Provision shared Dolt remote for `sdplab`
   - Configure `DOLT_REMOTE_USER` and `DOLT_REMOTE_PASSWORD`
   - Obtain remote URL (e.g., `https://doltremoteapi.dolthub.com/org/sdplab-beads`)

2. **Cutover Execution** (once infra is ready):
   ```bash
   # Configure remote
   scripts/beads_dolt_remote_bootstrap.sh --url <remote> --push

   # Verify cutover
   ./scripts/beads_transport.sh status
   # Expected: mode=dolt-remote remote_configured=true
   ```

3. **Documentation Updates**:
   - Update `docs/runbooks/beads-transport.md` with cutover runbook
   - Document hydration safety checks
   - Add bootstrap diagnostics guide

### Technical Debt Tracking

This workstream explicitly tracks the Dolt remote cutover as tech debt:
- Original implementation used git-backed archival export
- Dolt remote support was added but not cut over due to infra gap
- Clean cutover path preserved via `beads_transport.sh` mode detection

## Notes

- The primary blocker for the real cutover is infra provisioning of a shared Dolt remote. Follow-on tasks around hydration safety and bootstrap diagnostics were tracked under refs that became stale after compaction; use sdplab-cc4c as canonical tracking bead.
- Repo-side bootstrap is done. The remaining blocker is infra input, not missing repo code.
- Until the shared remote exists, `origin/beads-backup` remains only an archival export path, not a safe hydration path.
