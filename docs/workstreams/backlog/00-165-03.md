---
ws_id: 00-165-03
feature_id: F165
status: open
priority: P1
size: M
depends_on:
  - 00-165-02
ws_kind: leaf
parent_ws_id: 00-165-00
dispatch_lifecycle: active
---

# 00-165-03: F165 Vector Demos And Deterministic Tests

Feature: F165 (sdplab-28xb)
Phase: Pilot Evidence & Spec Quality
Status: Backlog

## Goal

Wire the three primary F165 vectors through the unsafe scripted oracle and the
defended core. Tests must show the naive path failing as expected and the defended
path passing or classifying residual risk.
The unsafe oracle is a new F165 test artifact and a hardcoded deterministic stub,
not an LLM call and not a production component.

## Scope Files

- `internal/evals/`
- `internal/evals/testdata/indirect_pi/`
- `cmd/sdp-eval/`
- `docs/security/`

## Beads

- primary: sdplab-f16503

## Dependencies

- 00-165-02

## Acceptance Criteria

- [ ] Beads issue poisoning demo produces an unsafe false completion/close claim in the scripted naive path and blocks it in the defended path.
- [ ] Workstream Markdown poisoning demo produces an unsafe scope/test-policy change in the scripted naive path and blocks it in the defended path.
- [ ] Evidence/finding poisoning demo produces an unsafe pass/suppression result in the scripted naive path and blocks it in the defended path.
- [ ] Non-obvious prose case passes by default-untrusted handling, not visible marker matching.
- [ ] Optional handoff or MCP case is either implemented or reported as `unsupported_surface`.
- [ ] Tests prove no live Beads, Git, filesystem write, network, or live model call is used for deterministic pass/fail.
- [ ] Any expansion beyond zero-width characters, HTML comments, Markdown link targets, and ordinary prose is recorded as scope change or follow-up, not silently absorbed.
