---
ws_id: 00-053-42
feature_id: F053
status: done
priority: P2
size: M
depends_on: []
---

# 00-053-42: Path Validation Consistency

Feature: F053 (sdp_dev-wuxy)  
Bead: sdp_dev-cugd (review finding)

## Goal

Apply path validation (SafePath, filepath.Clean, reject "..") consistently across all user-supplied paths. guard.rules_file and evidence.log_path are validated; sdp-evidence inspect path and other entry points may not be.

## Beads

- Historical feature bead: `sdp_dev-wuxy`
- Historical review finding bead: `sdp_dev-cugd`
- sdplab-cit: Bucket C5 tracker for legacy `## Beads` normalization in `00-053-41` ... `00-053-46`

## Scope Files

- `cmd/sdp-evidence/main.go`
- `internal/evidence/inspect.go`
- `internal/orchestrate/constraints.go`
- `internal/config/` (paths in config)
- Any code accepting file paths from CLI or config

## Dependencies

None. May overlap with 00-053-17 (sdp-evidence path validation); coordinate if both open.

## Acceptance Criteria

- [x] Audit all user-supplied path entry points
- [x] Each path validated: resolve under project root or CWD, reject ".." and absolute outside allowed
- [x] Reuse PathValidator or SafePath helper where applicable
- [x] go build ./... passes
- [x] go test ./... passes

---

## Execution Report

**Completed:** 2026-02-25

**Audit — user-supplied path entry points:**

| Entry Point | Path(s) | Validation |
|-------------|---------|-------------|
| sdp-evidence | validate/inspect --evidence | evidence.ValidatePath (00-053-17) |
| sdp-guard | --file (check-constraints) | evidence.ValidatePath (added) |
| sdp-eval | --project-root, --cases-dir | evidence.ValidatePath (added) |
| sdp-ci-loop | --checkpoint-dir, --runs-dir | evidence.ValidatePath (added) |
| sdp-orchestrate | --checkpoint-dir, --runs-dir | evidence.ValidatePath (added) |
| internal/config (sdp) | log_path, rules_file | validatePathWithinRoot (00-053-09, 00-053-28) |
| orchestrate/constraints | Constraint.Path from YAML | isPathSafeForConstraint (00-053-24) |

**Deliverables:**
- cmd/sdp-guard: validate --file under projectRoot before CheckFileAccess
- cmd/sdp-eval: validate projectRoot under CWD, casesDir under projectRoot
- cmd/sdp-ci-loop: validate checkpoint-dir and runs-dir under CWD
- cmd/sdp-orchestrate: validate checkpoint-dir and runs-dir under projectRoot
- All use evidence.ValidatePath (reused from 00-053-17)
- go build ./... passes

**Existing work summary:** sdp-evidence already validated (00-053-17). Config paths validated in sdp-plugin (00-053-09, 00-053-28). constraints has isPathSafeForConstraint. Extended validation to sdp-guard, sdp-eval, sdp-ci-loop, sdp-orchestrate.
