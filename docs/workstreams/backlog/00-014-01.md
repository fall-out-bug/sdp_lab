# WS-014-01: Telemetry analyzer agent core loop

Feature: FR-014
Priority: P1
Effort: 2d
Dependencies: FR-017 (telemetry collected)

## Goal

Implement cmd/telemetry-analyzer/ that subscribes to closed issues and telemetry, analyzes patterns, generates backlog proposals.

## Acceptance Criteria

- [x] cmd/telemetry-analyzer/main.go with configurable interval
- [x] Subscribes to NATS sdp.beads.*.closed
- [x] Reads .sdp/evidence/ for closed runs
- [x] LLM analysis via configurable model
- [x] Creates Beads issues tagged source:telemetry-analyzer
- [x] Rate limiting: max N proposals per cycle
- [x] Duplicate detection against existing open issues
- [x] K8s Deployment manifest

## Execution Report

- **2026-02-22**: Implemented cmd/telemetry-analyzer, NATS subscription to sdp.beads.*.closed, evidence read, LLM analysis (OpenRouter), bd create with source:telemetry-analyzer, rate limit (max 5/30min), duplicate detection (bd list --status open), K8s Deployment + Dockerfile with bd.

## Review Results (2026-02-22)

| Check | Result | Notes |
|------|--------|-------|
| Test coverage | FAIL | internal/telemetry 42.4% < 80% → sdp_dev-bo3 |
| Build | PASS | go build ./cmd/telemetry-analyzer |
| Kustomize | PASS | deploy/k8s/control |
| Duplicate detection | PASS | bd list --status open |
| Rate limit | PASS | max 5/30min cycle |

**Verdict:** CHANGES_REQUESTED (coverage)
