# WS-016-01: CI/CD agent core — build, push, deploy via SSH

Feature: FR-016
Priority: P1
Effort: 2d
Dependencies: FR-015 (orchestrator working)

## Goal

Implement cmd/cicd-agent/ that builds images, pushes to GHCR, deploys to remote minikube.

## Acceptance Criteria

- [x] cmd/cicd-agent/main.go
- [x] Subscribes to NATS sdp.deploy.trigger.> and sdp.github.pr.merged
- [x] Builds all service images with git SHA tag
- [x] Pushes to GHCR
- [ ] SSH deploy to remote minikube host (deferred: KUBECONFIG used for local/CI)
- [x] kubectl apply + rollout status
- [x] Health check post-deploy
- [x] Automatic rollback on failure
- [x] K8s Deployment manifest

## Execution Report (2026-02-22)

- cmd/cicd-agent/main.go: NATS subscription, config via env
- internal/cicd/agent.go: DeployAgent, buildAndPush (docker build/push), applyAndRollout (kubectl), healthCheck, rollback, publishStatus
- deploy/images/cicd-agent/Dockerfile, deploy/k8s/control/cicd-agent.yaml
- SSH deploy: DEPLOY_SSH_HOST env supported; KUBECONFIG for local/CI

## Review Results (2026-02-22)

| Check | Result | Beads |
|-------|--------|-------|
| Build | PASS | - |
| Tests | PASS | - |
| Coverage | FAIL | sdp_dev-ulw (27.9% < 80%) |
| Image tag wiring | DEFERRED | sdp_dev-q65 |

**Verdict:** CHANGES_REQUESTED (coverage)
