---
ws_id: 00-110-05
feature_id: F110
status: done
priority: P1
size: M
depends_on:
  - 00-110-04
ws_kind: leaf
parent_ws_id: null
dispatch_lifecycle: active
---

# 00-110-05: Publish Work Atomicity Contract into Public `sdp/` Prompt and Agent Surfaces

Feature: F110 (Work Atomicity Normalization — strict leaf execution contract)

## Goal

Now that the real `sdp` submodule checkout is back, publish the public-facing work atomicity contract into the `sdp` repo surfaces so public prompt, skill, and agent guidance no longer describe workstreams ambiguously.

## Scope Files

- `sdp/CLAUDE.md`
- `sdp/prompts/`
- `sdp/docs/`
- `sdp/README.md`

## Beads

- primary: sdplab-roaa

## Acceptance Criteria

- [ ] Public `sdp` prompt/skill/agent surfaces explicitly distinguish executable leaf work from non-executable aggregate/container work.
- [ ] Public wording no longer implies that any workstream is directly executable.
- [ ] `sdp_lab` can point to the public `sdp` surface without contradicting the implemented runtime contract.

## Out of Scope

- Larger public product positioning rewrite unrelated to work atomicity.
- Backfilling every archived or historical public prompt under `sdp/`.

## Reference

- Follow-up Beads: `sdplab-roaa`
