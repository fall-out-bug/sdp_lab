---
ws_id: 00-110-04
feature_id: F110
status: done
priority: P1
size: M
depends_on:
  - 00-110-03
ws_kind: leaf
parent_ws_id: null
dispatch_lifecycle: active
---

# 00-110-04: Restore Real `sdp/` Submodule Checkout for Public Protocol Publishing

Feature: F110 (Work Atomicity Normalization — strict leaf execution contract)

## Goal

Repair the repo boundary so `sdp_lab` stops lying about a public `sdp/` protocol checkout that does not actually exist. This slice must restore `sdp` from a tracked binary blob back to a real git submodule checkout, or otherwise make the boundary docs brutally honest if full restoration proves impossible.

## Scope Files

- `.gitmodules`
- `sdp`
- `AGENTS.md`
- `docs/MULTI-REPO-WORKFLOW.md`
- `docs/architecture/REPO-BOUNDARY.md`

## Beads

- primary: sdplab-ycv8

## Acceptance Criteria

- [ ] Repository root no longer tracks `sdp` as an executable blob; `git ls-tree HEAD sdp` shows a gitlink again.
- [ ] `git submodule status` reports a real `sdp` checkout instead of silently succeeding with no submodule.
- [ ] Repo boundary docs no longer describe an impossible `sdp/` editing flow.
- [ ] F110 can honestly publish public prompt/skill/agent artifacts through the documented `sdp` path again.

## Out of Scope

- Actual publication of new work atomicity prompt/skill content into `sdp`.
- General cleanup of all historical docs that reference `sdp/`.

## Reference

- Follow-up Beads: `sdplab-ycv8`
- Source of truth contract: `AGENTS.md`, `docs/MULTI-REPO-WORKFLOW.md`, `docs/architecture/REPO-BOUNDARY.md`
