# WS-013-01: Provider health check and subscription routing

Feature: FR-013
Priority: P1
Effort: 1d
Dependencies: WS-012-01

## Goal

Add provider health check and routing preference for direct subscriptions over OpenRouter.

## Acceptance Criteria

- [x] ProviderHealthChecker interface implemented
- [x] Checks quota remaining for subscription providers
- [x] Routes to direct subscription when available
- [x] Falls back to OpenRouter → GLM
- [x] Env var propagation in Task CRD spec for agent pods
- [x] Telemetry: provider used, cost saved

## Execution Report (2026-02-22)

**Implemented:**
- `internal/policy/health.go`: ProviderHealthChecker interface, StubProviderHealthChecker, ResolveProviderForModel (subscription→openrouter→glm)
- `internal/adapter/agentrun_helpers.go`: createTaskFromIntent accepts providerHint, adds annotation sdp.provider
- `internal/adapter/agentrun_reconciler.go`: ProviderHealthChecker in opts, ResolveProviderForModel when creating Tasks
- `cmd/adapter-controller/main.go`: StubProviderHealthChecker wired (real quota check deferred)
- `internal/policy/health_test.go`: StubProviderHealthChecker, ResolveProviderForModel tests
- Telemetry for provider/cost deferred to follow-up

## Execution Report (2026-02-22) — Telemetry

- observability: sdp_provider_used_total (project, provider, model), sdp_cost_saved_usd (project, provider)
- agentrun_reconciler: IncProviderUsed, AddCostSaved ($0.001/run when direct subscription)
