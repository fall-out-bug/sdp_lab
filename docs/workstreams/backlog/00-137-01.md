---
ws_id: 00-137-01
feature_id: F137
status: open
priority: P1
size: S
depends_on: []
---

# 00-137-01: Command inventory + contract freeze

Feature: F137 (CLI Surface Normalization)

## Goal

Produce one authoritative inventory of the current CLI surface so future registry work is driven by explicit keep/shim/retire decisions instead of folklore.

## Beads

- sdplab-pg7o.1

## Scope Files

- `docs/reference/cmd-inventory.md` — new inventory and disposition matrix
- `docs/plans/2026-04-20-sdp-framework-normalization-design.md` — confirm feature boundaries if inventory exposes contradictions

## Acceptance Criteria

- [ ] Every `cmd/sdp-*` binary and `cmd/sdp/` subcommand in scope is listed
- [ ] Each entry is classified as `keep as subcommand`, `shim`, `retire`, or `out-of-scope`
- [ ] Ownership collisions with `F134`, `F125`, and `F126` are called out explicitly
- [ ] The inventory names the initial high-value commands for registry migration

## Out of Scope

- Implementing the registry
- Migrating command behavior
- Writing shims
