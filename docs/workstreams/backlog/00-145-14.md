---
ws_id: 00-145-14
feature_id: F145
status: open
priority: P1
size: M
depends_on: [00-145-12, 00-145-13, 00-145-07]
---

# 00-145-14: smoke integration test + Day-8 acceptance demo

Feature: F145 (Multi-Provider Dispatch Matrix & Confidence-Driven Cascade)

Design reference: [2026-04-26-f145-multi-provider-dispatch-cascade-design.md §8](../../plans/2026-04-26-f145-multi-provider-dispatch-cascade-design.md)

## Goal

Завершающая WS: интеграционный smoke test через `workflow_dispatch` (real CLIs + secrets) + публичный Day-8 acceptance demo (cascade-replay corpus прогон + CSV report артифакт).

## Beads

- sdplab-2p8g (historical leaf issue)

## Scope Files

- `.github/workflows/dispatch-smoke.yml` — new. `workflow_dispatch` only. Steps: install codex/cursor/opencode CLIs, set secrets (`OPENAI_API_KEY`, `ANTHROPIC_API_KEY`, `KIMI_API_KEY`, `CURSOR_API_KEY`), run `cmd/sdp-cascade-replay --live --corpus testdata/cascade_corpus.yaml`. Upload CSV report as artifact.
- `internal/dispatch/cascade/smoke_test.go` — new (build tag `//go:build integration`). Реальные harness'ы (если CLI на PATH), trivial prompt на каждый из 4 harness'ов, assertion на exit_code=0.
- `cmd/sdp-harness/main.go` — MODIFY. Bootstrap `OllamaProvider`, `OpenAIProvider`, `AnthropicProvider`, `CursorProvider`, `KimiProvider` в default registry; bootstrap `LimitsCache`.
- `docs/reference/cascade-routing.md` — new. End-user doc: как настроить cascade, как читать report, как добавить кастомный gate.
- `docs/workstreams/INDEX.md` — MODIFY (если ещё не покрыто на этапе design). Sanity check.

## Acceptance Criteria

- [ ] `dispatch-smoke.yml` workflow зелёный (или красный с понятным сообщением если secrets не выставлены).
- [ ] Smoke integration test (`-tags=integration`) запускается локально и проходит для всех 4 harness'ов когда CLIs доступны.
- [ ] `cmd/sdp-cascade-replay --live` производит CSV с реальными data из 4 провайдеров.
- [ ] CSV report отображает: `% stayed_cheap`, `% escalated_to_strong`, `% failed_all`, `avg_tokens`, `avg_latency`.
- [ ] Day-8 acceptance: на 20-prompt corpus ≥40% staying-on-cheap для simple-coding-easy сегмента (mock-replay в WS 12 уже это проверяет; здесь — на live data target = 30%).
- [ ] Все existing CI gates (`run_go_quality_gates.sh`) зелёные.
- [ ] `docs/reference/cascade-routing.md` покрывает все API: Policy, ShortCircuitConfig, GateFunc, custom tier'ы.

## Out of Scope

- Метрики в production observability (F140 telemetry pipeline integration).
- Dashboard.
- Auto-publishing report в Slack/Discord (manual review).
