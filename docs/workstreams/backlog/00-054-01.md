---
ws_id: 00-054-01
feature_id: F054
status: done
priority: P2
size: M
depends_on: []
---

# 00-054-01: @build — post-build bd close + batch /build

Feature: F054 (sdp_dev-hryg)

## Goal

Add to @build skill: (1) After successful build, run `bd close <bead_id>` for each bead in WS scope. (2) Support batch `/build 00-053-16..25` — sequential execution with stop on first failure.

## Beads

- sdplab-jb5: Final Bucket C tracker for legacy `## Beads` normalization in `F054` ... `F056`
- sdp_dev-hryg: @build post-build bd close + batch

## Scope Files

- `sdp/prompts/skills/build/SKILL.md`
- `.cursor/skills/build/SKILL.md` (if exists)

## Acceptance Criteria

- [x] @build skill: after success, "Run bd close <bead_id> for each bead in WS frontmatter"
- [x] @build skill: document batch syntax `/build 00-053-16..25` (or equivalent)
- [x] Batch executes sequentially; stops on first failure; reports N done, M failed

---

## Execution Report

**Date:** 2026-02-25

**Summary:** Updated sdp build skill with post-build bd close and batch execution.

**Changes:**
- `sdp/prompts/skills/build/SKILL.md`:
  - Beads Integration Success: "Run bd close for each bead in WS frontmatter" with resolution from .beads-sdp-mapping.jsonl or WS body
  - Batch syntax in intro: `/build 00-053-16..25` (or multiple WS IDs)
  - New "Batch Execution" section: expand range, sequential, stop on first failure, report N done M failed
  - Version 8.1.0, F054 changes

**Existing work:** Beads Integration already had bd close; post-build.sh uses .beads-sdp-mapping.jsonl. No .cursor/skills/build exists.
