---
ws_id: 00-133-01
feature_id: F133
status: open
priority: P3
size: M
depends_on: []
---

# 00-133-01: Local model dispatch — route low-complexity coding to Ollama

Feature: F133 (Local model dispatch)

Beads: sdplab-8wu

Backfilled 2026-04-26 under F142-01. Auto-generated scaffold from beads description; refine via `/feature` regen if scope expands.

## Goal

Add a `local` tier to the dispatch pipeline so SDP can route low-complexity coding tasks to a local Ollama model instead of always dispatching to a cloud provider. Cost and privacy are the primary drivers; cloud fallback remains for everything else.

## Scope Files

- `internal/dispatch/router.go` — extend `Router` to recognise a `local` tier alongside the existing cloud dispatch path.
- `internal/dispatch/classify.go` — extend `TaskClassification` so `(Complexity=low, RequiredCap=coding)` resolves to `local`.
- `internal/dispatch/local_ollama.go` — new. Thin Ollama HTTP client (no model bundling — assumes user-managed Ollama instance on `localhost:11434` or `OLLAMA_HOST`).
- `configs/dispatch.example.yaml` — document the `local` tier and how to enable it.
- `cmd/sdp/cmd_dispatch.go` — wire `--prefer-local` flag (or env) for opt-in.

## Acceptance

- A task classified as `(Complexity=low, RequiredCap=coding)` dispatches to local Ollama when enabled, falls back to cloud on local error.
- Disabled by default (no behaviour change for users without Ollama installed).
- Unit test on classifier: low+coding → local tier when enabled.
- Integration test stubbed (real Ollama not required in CI).
- Documentation describes setup, fallback behaviour, and how to disable.

## Notes

- Focus on dispatch *integration*, not on bundling models. Ollama lives outside SDP.
- F133 existed only as a beads epic with no executable leaf; F142-01 audit flagged it as picker bait. This file unblocks `/build`.
