---
ws_id: 00-F130-05
feature_id: F130
title: Bootstrap integration — wire into sdp bootstrap and add drift detection
status: open
priority: P2
size: M
depends_on: ["00-F130-04"]
repo: sdp_lab
---

# 00-F130-05: Bootstrap integration — wire into sdp bootstrap and add drift detection

Feature: F130 (AI Harness Config Auto-Provisioning)

## Goal

Wire the full pipeline (pattern extraction → rules generation → harness adapters) into `sdp bootstrap` (F124 flow). Support three lifecycle stages: greenfield (generate from scratch), brownfield-new (generate + respect existing), brownfield-mature (diff against current and update only deltas). Add `sdp rules update` subcommand for re-running on mature repos.

## Scope Files

- `internal/bootstrap/rules_provisioner.go` (new — integrates into bootstrap)
- `cmd/sdp/main.go` (add `rules update` subcommand)
- `docs/reference/go-patterns.md` (update note: "auto-generated by sdp rules update")
- `sdp/docs/QUICKSTART.md` (update bootstrap section)

## Acceptance Criteria

- [ ] `sdp bootstrap` runs rules provisioning as a phase
- [ ] `sdp rules update` re-runs on mature repo and produces diff
- [ ] Greenfield: all harness configs generated
- [ ] Brownfield-mature: only deltas applied, existing content preserved
- [ ] QUICKSTART.md documents the new bootstrap step
