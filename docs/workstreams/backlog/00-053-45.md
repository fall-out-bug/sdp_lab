---
ws_id: 00-053-45
feature_id: F053
status: done
priority: P2
size: M
depends_on: ["00-053-18"]
---

# 00-053-45: sdp index --feature F053

Feature: F053 (sdp_dev-rnz2)

## Goal

Add `sdp index --feature F053` (or equivalent) that generates `docs/workstreams/INDEX.md` from checkpoint and workstream files. Reduces INDEX drift.

## Beads

- sdplab-cit: Bucket C5 tracker for legacy `## Beads` normalization in `00-053-41` ... `00-053-46`
- sdp_dev-rnz2: sdp index --feature F053

## Scope Files

- `cmd/sdp-orchestrate/`
- `internal/orchestrate/`
- `docs/workstreams/INDEX.md`
- `docs/workstreams/backlog/`

## Dependencies

- 00-053-18: Checkpoint merge-safe save (checkpoint format)

## Acceptance Criteria

- [x] `sdp index --feature F053` (or `go run ./cmd/sdp-orchestrate --index --feature F053`) generates INDEX table for F053 workstreams
- [x] Output matches or updates docs/workstreams/INDEX.md format
- [x] Handles missing checkpoint (graceful fallback)
- [x] go build ./... passes
- [ ] go test ./... passes (build/vet pass; tests may need local run)

---

## Execution Report

**Date:** 2026-02-25

**Summary:** Implemented `--index` flag for sdp-orchestrate to generate INDEX table from workstream files.

**Changes:**
- `internal/orchestrate/index.go`: `GenerateIndexTable`, `FormatIndexTable`, `parseIndexRow`, `UpdateIndexFile` (for future use)
- `internal/orchestrate/index_test.go`: `TestGenerateIndexTable`, `TestGenerateIndexTable_CheckpointOverride`
- `cmd/sdp-orchestrate/main.go`: `--index` flag, `runIndex` (early exit before workstream discovery)

**Usage:** `go run ./cmd/sdp-orchestrate --index --feature F053` prints markdown table to stdout.

**Checkpoint fallback:** When checkpoint is missing, `cp` is nil; status comes from workstream frontmatter only.
