---
ws_id: 00-120-04
feature_id: F120
status: done
priority: P1
size: S
depends_on:
  - 00-120-03
---

# 00-120-04: Review Remediation — Context, Branch Detection, Contract Fields

Feature: F120 (Toolkit Scout — instant repo card for unknown codebases)

## Goal

Fix three blocking P1 findings from round-1 review: git commands without timeout/context cancellation, hardcoded "main" branch assumption, and unpopulated contract fields that create design-implementation drift.

## Beads

- sdplab-7uke

## Scope Files

- `internal/scout/activity.go`
- `internal/scout/scout.go`
- `internal/scout/identity.go`
- `internal/scout/health.go`
- `internal/scout/languages.go`

## Dependencies

- 00-120-03: CLI surfaces must be complete before remediation

## Acceptance Criteria

- [ ] `gitCmd` uses `context.Context` with timeout (30s default) — no bare `exec.Command` without cancellation
- [ ] `scout.Run` accepts `context.Context` and threads it through to git calls
- [ ] branch detection tries `main` then `master` then falls back to HEAD — never silently returns 0
- [ ] `RepoURL` is populated from `git remote get-url origin` (or nil if no remote)
- [ ] `ConfigFiles` is populated with CI/config file paths found during identity phase
- [ ] `DependencyCount` is populated from go.mod require count or equivalent for detected build system
- [ ] all existing tests still pass
- [ ] new tests cover context cancellation and branch fallback scenarios

## Out of Scope

- P2/P3 findings (WalkDir migration, Go modernization, bus factor improvement)
- GoReleaser packaging (DevOps P2)
- Standalone CLI documentation (Docs P2)

## Implementation Notes

- Review findings source: `.sdp/review_verdict.json` round 1
- Blocking IDs: B1 (activity.go:93), B2 (activity.go:57), B3 (types.go:25,88,90)
- Keep changes minimal — focused on the three P1 fixes only
