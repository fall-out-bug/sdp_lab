---
ws_id: 00-053-23
feature_id: F053
status: done
priority: P2
size: L
depends_on: []
---

# 00-053-23: SRE Context Propagation

Feature: F053 (sdp_dev-jx39)  
Beads: sdp_dev-luyz, sdp_dev-06a4, sdp_dev-59z6, sdp_dev-pmod

## Goal

Fix context propagation: time.After leak, poller ctx, exec CommandContext, sdp apply signals.

## Scope Files

- `internal/ciloop/loop.go`
- `internal/ciloop/poller.go`
- `sdp/sdp-plugin/cmd/sdp/apply.go`
- `sdp/sdp-plugin/cmd/sdp/build.go`
- `internal/orchestrate/policy.go`
- `internal/evidence/auto_attest.go`
- `internal/orchestrate/hydrate_sources.go`

## Dependencies

None.

## Acceptance Criteria

- [x] time.After → time.NewTimer + defer timer.Stop() in ciloop loop
- [x] Poller.GetChecks accepts ctx; retries check ctx.Done()
- [x] sdp apply uses signal.NotifyContext for SIGINT/SIGTERM
- [x] exec.Command → CommandContext in build, policy, auto_attest
- [x] go build ./... passes
- [x] go test ./... passes

---

## Execution Report

**Date:** 2026-02-25  
**Verdict:** PASS

- **time.After:** Replaced with time.NewTimer + select; timer.Stop() on ctx.Done in loop.go (PollDelay, RetryDelay).
- **Poller:** GetChecks(ctx, prNumber); checks ctx.Done() before each attempt and before retry sleep; timer for sleep.
- **sdp apply:** signal.NotifyContext(SIGINT, SIGTERM) + defer stop().
- **sdp build:** signal.NotifyContext; exec.CommandContext for pre-build, go test, post-build.
- **policy:** EvaluatePolicies(ctx, ...); queryOPAString/Set use CommandContext; main_advance passes advanceCtx.
- **auto_attest:** AutoAttest(ctx, opts); runGit, collectTestResults, collectLintResults use CommandContext; git helpers accept ctx.
- hydrate_sources: not in AC list; left as-is.
