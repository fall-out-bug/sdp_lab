---
ws_id: 00-145-05
feature_id: F145
status: open
priority: P2
size: M
depends_on: [00-145-01]
---

# 00-145-05: KimiProvider — Moonshot

Feature: F145 (Multi-Provider Dispatch Matrix & Confidence-Driven Cascade)

Design reference: [2026-04-26-f145-multi-provider-dispatch-cascade-design.md §4.1](../../plans/2026-04-26-f145-multi-provider-dispatch-cascade-design.md)

## Goal

Реализовать `KimiProvider` для Moonshot моделей. Доступ через opencode CLI с `--provider kimi --model <name>`. Provider — metadata-only.

## Beads

- sdplab-60ia (historical leaf issue)

## Scope Files

- `internal/dispatch/harness/providers/kimi.go` — new. `KimiProvider struct{}`, `Name() = "kimi"`, `Models()` возвращает: `["kimi-k1.5", "kimi-k2", "moonshot-v1-8k", "moonshot-v1-32k", "moonshot-v1-128k"]`. `CheckLimits` parses `x-ratelimit-*` если Moonshot их шлёт; иначе stub с `Source: "kimi-config"`.
- `internal/dispatch/harness/providers/kimi_test.go` — new. Tests: Models non-empty, headers parsing.

## Acceptance Criteria

- [ ] `Models()` содержит ≥ 4 моделей Moonshot/Kimi.
- [ ] `Name() == "kimi"` (отдельно от ZAI/GLM).
- [ ] go build, go test, go vet clean.
- [ ] Test coverage ≥85%.

## Out of Scope

- Profile entries — WS 09.
- Прямой Moonshot SDK — все запросы через opencode CLI.
