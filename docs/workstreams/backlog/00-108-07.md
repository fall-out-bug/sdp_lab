---
ws_id: 00-108-07
feature_id: F108
status: backlog
priority: P1
size: XS
depends_on: []
---

# 00-108-07: Документация ролей — modelgateway, ServeBridge, agentloop

Feature: F108

## Goal

Зафиксировать архитектурные роли трёх компонентов через README и doc-комментарии. Устранить неопределённость судьбы `modelgateway` и исключить путаницу между ServeBridge и agentloop.

## Beads

- sdplab-6tf6

## Scope Files

- `internal/modelgateway/README.md` — создать (статус, роль, когда подключать)
- `internal/executor/bridge_serve.go` — добавить/обновить package/type comment
- `internal/agentloop/harness.go` — добавить/обновить package comment

## Acceptance Criteria

- [ ] `internal/modelgateway/README.md` содержит: статус "0 production callers", роль (enterprise credential management), когда подключать, чем отличается от `llmclient`
- [ ] `internal/executor/bridge_serve.go` имеет comment с явным разграничением: ServeBridge = делегирование внешнему харнессу, agentloop = SDP сам является агентом
- [ ] `internal/agentloop/harness.go` имеет package comment с ролью: "SDP's internal LLM agent loop"
- [ ] `go build ./...` успешен (изменения только в комментариях и README)

## Out of Scope

- Изменение кода (только комментарии и docs)
- Удаление modelgateway

## Implementation Notes

Подробные шаги — в `docs/plans/2026-04-11-f108-architecture-normalization-plan.md`, раздел WS-07.

Ключевое утверждение для всех трёх мест:

| Компонент | Роль | Статус |
|-----------|------|--------|
| `agentloop.Harness` | SDP сам является агентом (внутренний LLM loop) | Production после F108 |
| `executor.ServeBridge` | SDP делегирует внешнему харнессу (Claude Code, Cursor, opencode) | Production |
| `modelgateway` | Enterprise credential management поверх llmclient | 0 callers, future |
