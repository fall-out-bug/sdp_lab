---
ws_id: 00-121-05
feature_id: F121
status: done
priority: P2
size: M
depends_on:
  - 00-121-03
---

# 00-121-05: Robustness and Performance

Feature: F121 (Toolkit Metrics — git-derived process health)

## Goal

Fix 5 robustness and performance findings from the F121 review: add repo path validation, fix N+1 branch fetching, improve gitCmd error handling, optimize gini() from O(n^2) to O(n log n), and propagate context through collection calls.

## Beads

- (to be created)

## Scope Files

- `internal/metrics/collector.go`
- `internal/metrics/knowledge.go`

## Review Findings Addressed

| # | Severity | Source | Finding |
|---|----------|--------|---------|
| 1 | P2 | security | No repoPath validation |
| 2 | P2 | devops | N+1 in parseBranches |
| 3 | P2 | devops | gitCmd swallows errors |
| 4 | P2 | sre | gini() O(n^2) |
| 5 | P2 | sre | Context cancellation only at start |

## Acceptance Criteria

- [x] Collect/CollectWithContext validate repoPath: check directory exists and contains `.git` (or is a git repo via `git rev-parse`). Return descriptive error if invalid.
- [x] collectBranches uses a single batch `git log` to fetch dates for all branches instead of N individual git calls
- [x] gitCmd returns structured errors (including exit code and stderr) instead of swallowing with `fmt.Errorf`
- [x] gini() uses sorted array approach (O(n log n)) instead of nested loops
- [x] Context is checked between git command calls in the collection pipeline (not just at start)
- [x] go build ./... passes
- [x] go test ./internal/metrics/... passes
