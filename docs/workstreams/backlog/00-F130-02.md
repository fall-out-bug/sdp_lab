---
ws_id: 00-F130-02
feature_id: F130
title: sdp-healthcheck CLI — dev environment health checker
status: done
priority: P1
size: M
depends_on: ["00-F130-01"]
repo: sdp_lab
---

# 00-F130-02: sdp-healthcheck CLI — dev environment health checker

Feature: F130 (Agent Code Quality Standards)

## Goal

New binary cmd/sdp-healthcheck with 3 checks: go-build, beads-ready, git-clean. --json and --check flags. Business logic in internal/healthcheck/.

## Scope Files

- `cmd/sdp-healthcheck/main.go` — CLI entry point (≤60 lines)
- `internal/healthcheck/types.go` — shared types
- `internal/healthcheck/healthcheck.go` — runner logic
- `internal/healthcheck/checks.go` — individual check implementations
- `internal/healthcheck/*_test.go` — unit and integration tests

## Acceptance Criteria

- [ ] go build ./cmd/sdp-healthcheck/... succeeds
- [ ] go test ./internal/healthcheck/... passes
- [ ] cmd/main.go ≤60 lines
- [ ] integration tests use testing.Short() guard
