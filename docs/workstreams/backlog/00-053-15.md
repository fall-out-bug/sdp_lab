---
ws_id: 00-053-15
feature_id: F053
status: done
priority: P2
size: M
depends_on: []
---

# 00-053-15: Verifier ctx + Evidence test + slog + Config timeouts

Feature: F053 (sdp_dev-v2hm)

## Goal

Fix Verifier context, Evidence test flakiness, slog migration, config timeouts. See docs/drafts/idea-phase4-dorabotka.md.

## Scope Files

- `sdp/sdp-plugin/internal/verify/verifier.go`
- `sdp/sdp-plugin/internal/evidence/`
- `sdp/sdp-plugin/internal/config/`
- `sdp/schema/config.schema.json`

## Dependencies

None.

## Acceptance Criteria

- [x] Verifier: context propagation, no defer-in-loop leak (sdp-y7dg)
- [x] Evidence: TestEmit_EventuallyWrites — reduce flakiness (sdp-yout)
- [x] Logging: slog migration for evidence + executor (sdp-s3vc)
- [x] Config: timeouts section + SDP_TIMEOUT_* env override (sdp-ycsn)
- [x] go test ./... passes

## Beads

- sdplab-6uq: Bucket C2 tracker for legacy `## Beads` normalization in `00-053-11` ... `00-053-20`

| Bead | Description |
|------|-------------|
| sdp-y7dg | Verifier ctx |
| sdp-yout | Evidence test flakiness |
| sdp-s3vc | slog migration |
| sdp-ycsn | Config timeouts |

---

## Execution Report (2026-02-25)

**Verdict:** PASS

**Changes:**
- `sdp/sdp-plugin/internal/verify/verifier.go`: defer cancel() in VerifyCommands loop (fix context leak on SafeCommand error)
- `sdp/sdp-plugin/internal/evidence/emitter_test.go`: retry Stat in TestEmit_EventuallyWrites to reduce flakiness
- `sdp/schema/config.schema.json`: timeouts description (SDP_TIMEOUT_* env override)
- slog + config timeouts: already present per design

**Quality gates:** go test ./... pass
