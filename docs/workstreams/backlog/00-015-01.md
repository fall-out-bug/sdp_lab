# WS-015-01: Feature orchestrator core loop (replace swarm-orchestrator dispatch)

Feature: FR-015
Priority: P0
Effort: 2d
Dependencies: FR-002 (AgentRun CRD), FR-005 (NATS bridge)

## Goal

Implement cmd/feature-orchestrator/ that polls ready tasks, prioritizes, creates AgentRun CRDs.

## Acceptance Criteria

- [x] cmd/feature-orchestrator/main.go
- [x] Polls aggregator for ready tasks across projects
- [x] Priority queue: P0 > P1 > P2 > P3, then by age
- [x] Dependency check: all deps closed before dispatch (beads dep list + DepsClosed)
- [x] Creates AgentRun CRD via K8s client
- [x] RunLockManager prevents duplicates
- [x] ConfigMap: poll interval, max concurrent, project filter
- [x] K8s Deployment manifest

## Execution Report

**Date:** 2025-02-22
**Status:** DONE

- cmd/feature-orchestrator/main.go: polls aggregator, prioritizes P0>P1>P2>P3+age, creates AgentRun CRDs
- RunLockManager prevents duplicates; ConfigMap for poll/max/projects
- K8s Deployment, ServiceAccount, Role/RoleBinding in sdp-workers
- Tests: TestResolveModel, TestResolveWorkstream, TestParseProjectFilter, TestBuildAgentRun — PASS
- Dependency check deferred (beads dep API)

## Review Results (FR-015)

**Date:** 2026-02-22

| Reviewer | Verdict | Summary |
|----------|---------|---------|
| QA | FAIL | Coverage 23.8% (target 80%); beads: j56, q80, t5p, f4c |
| Security | FAIL | Path traversal in RunLockManager; beads: fis, rxt, c6z, f1m |
| DevOps | FAIL | CI build missing, RBAC wrong namespace; beads: vcm, ob7, 1mg, pye, 6f9, 8w6 |
| SRE | FAIL | No SLOs, no runbook; beads: 21l, 93p, 3dl, k6d |
| TechLead | FAIL | RunLockManager fs locks not shared; beads: rsd, m01, bih, zdr, 7sy |
| Docs | FAIL | ConfigMap project_filter missing, aggregator age/priority; beads: 8vq, rx6, 3xd, vya, km2 |

**Overall:** CHANGES_REQUESTED

### P0/P1 Fixes (2026-02-22)

- CI: feature-orchestrator image build added
- RBAC: Role/RoleBinding in sdp-workers (feature-orchestrator-rbac.yaml)
- LeaseLockManager: K8s Lease for distributed locking
- issueID validation: safeid.ValidateIssueID in RunLockManager (TryAcquire+Release) and LeaseLockManager
- ConfigMap: project_filter, env-driven poll/max/namespace
- Aggregator: priority ascending (P0 first), then age, then project
- leaseName: DNS-1123 sanitization (lowercase, alphanumeric, hyphens)

### P1 TechLead Fixes (2026-02-22)

- sdp_dev-bih: observability.SetupTracing and agg.Run(ctx) errors logged
- sdp_dev-m01: DispatchConfig struct (7 fields) extracted for dispatch()

### Dependency Check (2026-02-22)

- internal/beads/adapter.go: DepsClosed(issueID) — bd dep list + Show for each blocker
- internal/federation/bridge.go: pollReady filters issues with open deps before emit
