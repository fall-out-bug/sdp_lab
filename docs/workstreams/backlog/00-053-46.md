---
ws_id: 00-053-46
feature_id: F053
status: done
priority: P2
size: S
depends_on: []
---

# 00-053-46: Integration Test Contract in Hooks

Feature: F053 (sdp_dev-utmd)

## Goal

Document and enforce integration test contract: CI runs `go test -short ./...`; integration tests use `t.Skip()` or `testing.Short()`. Never delete integration tests to fix flakiness.

## Beads

- sdplab-utmd: legacy reference (done)

- sdp_dev-utmd: Integration test contract in hooks

## Scope Files

- `.sdp/pipeline-hooks.yaml.example`
- `AGENTS.md`
- `internal/orchestrate/hooks.go`

## Acceptance Criteria

- [x] Pipeline hooks or CI run `go test -short ./...` (or equivalent)
- [x] AGENTS.md documents: "Integration tests: use t.Skip() + -short; never delete; CI: go test -short"
- [x] Example in pipeline-hooks or README
- [x] go build ./... passes

---

## Execution Report

**Date:** 2026-02-25

**Summary:** Documented and enforced integration test contract across CI, sdp build, AGENTS.md, and pipeline-hooks.example.

**Changes:**
- `.github/workflows/ci.yml`: Test step uses `go test -short ./...`; coverage step uses `go test -short ./internal/...`
- `AGENTS.md`: Added "Integration tests" paragraph under Quality Gates
- `.sdp/pipeline-hooks.yaml.example`: Added comment block documenting the contract

**Existing work:** CI ran `go test ./... -count=1`; sdp build ran `go test ./...`; auto-attest main_test.go already uses `testing.Short()` for integration skip.
