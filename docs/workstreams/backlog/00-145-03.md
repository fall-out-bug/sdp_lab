---
ws_id: 00-145-03
feature_id: F145
status: open
priority: P1
size: M
depends_on: [00-145-01]
---

# 00-145-03: AnthropicProvider — Sonnet/Opus/Haiku catalog

Feature: F145 (Multi-Provider Dispatch Matrix & Confidence-Driven Cascade)

Design reference: [2026-04-26-f145-multi-provider-dispatch-cascade-design.md §4.1, §4.5](../../plans/2026-04-26-f145-multi-provider-dispatch-cascade-design.md)

## Goal

Реализовать `AnthropicProvider` с каталогом Anthropic моделей (через claude CLI и через cursor agent). Mirror `OpenAIProvider` шаблон — metadata + headers parsing.

## Beads

- (assigned during epic creation)

## Scope Files

- `internal/dispatch/harness/providers/anthropic.go` — new. `AnthropicProvider struct{}`, `Name() = "anthropic"`, `Models()` возвращает каталог: `["claude-opus-4-7", "claude-sonnet-4-6", "claude-haiku-4-5", "claude-opus-4-1", "claude-sonnet-4-5", "claude-haiku-4-1"]`. `CheckLimits` через cached state из `LimitsCache`. `UpdateFromHeaders` парсит `anthropic-ratelimit-*` headers.
- `internal/dispatch/harness/providers/anthropic_test.go` — new. Mirror openai_test.go.

## Acceptance Criteria

- [ ] `Models()` содержит ≥ 6 моделей actuel Anthropic lineup.
- [ ] `UpdateFromHeaders` корректно парсит `anthropic-ratelimit-*` (формат отличается от OpenAI).
- [ ] go build, go test, go vet clean.
- [ ] Test coverage ≥85%.

## Out of Scope

- HTTP-call к Anthropic API.
- Profile entries — WS 09.
