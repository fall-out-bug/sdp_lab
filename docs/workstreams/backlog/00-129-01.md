---
ws_id: 00-129-01
feature_id: F129
status: open
priority: P1
size: S
depends_on: []
---

# 00-129-01: Subagent default dispatch policy

Feature: F129 (Autonomous Operations & Regression Hardening)

## Goal

Update `AGENTS.md` и shared skill preamble so that subagent dispatch is the default for tasks requiring ≥3 file research or ≥2 independent subtasks. Provide a decision-tree that also covers when NOT to use subagents.

## Beads

- sdplab-8k5.1

## Scope Files

- `AGENTS.md` — add new section "Subagent dispatch policy"
- `.agents/skills/README.md` — reference the new policy
- `docs/reference/subagent-policy.md` — new doc with decision-tree

## Acceptance Criteria

- [ ] `AGENTS.md` policy section added with harness-neutral phrasing
- [ ] Decision tree: use subagent when — research ≥3 files OR ≥2 independent subtasks OR need fresh context
- [ ] Decision tree: don't use when — trivial lookup, single-file edit, need ongoing dialogue with user
- [ ] `docs/reference/subagent-policy.md` ≤80 lines
- [ ] No harness-specific phrases (passes `sdp-protocol-check --lint-skills` if applied)

## Out of Scope

- Training material / worked examples beyond the decision-tree
- Harness-specific CLI wiring

## Implementation Notes

Reference the `parallel-dispatch` skill (F129-02) for the parallel variant. Keep `AGENTS.md` change surgical: one new section, cross-link to `docs/reference/subagent-policy.md` for the full tree.
