# WS-011-01: Expand project-registry.yaml with 5 projects

Feature: FR-011
Priority: P0
Effort: 0.5d
Dependencies: none

## Goal

Add all 5 target projects to registry with correct repo URLs, branches, beads prefixes, and fork configuration.

## Acceptance Criteria

- [x] project-registry.yaml has sdp_dev, sdp, opencode, kubeopencode, openclaw, beads
- [x] Fork-based projects have `fork: true` and `upstream_remote` fields
- [x] registry-agent serves updated registry
- [x] `internal/registry/` supports new fields

## Execution Report (2026-02-22)

- specs/project-registry.yaml: 6 projects with fork/upstream_remote/upstream_url for opencode, kubeopencode, openclaw
- internal/registry/project.go: Fork, UpstreamRemote, UpstreamURL
- registry-agent: HTTP CRUD, loads from registry path
