# WS-003-02: Define AgentRun and Task CRD types in api/v1alpha1

Feature: FR-003 (sdp_dev-j25)
Priority: P0
Effort: 1d
Dependencies: WS-003-01

## Goal

Create Go type definitions for AgentRun CRD with kubebuilder markers. Define a Task CRD shim that wraps kubeopencode types (or provides a compatible interface until upstream types are available).

## Acceptance Criteria

- [x] `api/v1alpha1/types.go` exists with AgentRun spec/status structs
- [x] `api/v1alpha1/task_shim.go` — interface-compatible with kubeopencode Task
- [x] kubebuilder markers: `+kubebuilder:object:root=true`, `+kubebuilder:subresource:status`
- [x] `api/v1alpha1/groupversion_info.go` with SchemeBuilder
- [x] Types are compatible with internal/adapter/ (CRDPhase maps to shim phases)

## Execution Report (2026-02-22)

- api/v1alpha1/types.go: AgentRun, AgentRunSpec, AgentRunStatus, AgentRunList
- api/v1alpha1/task_shim.go: Task, TaskPhase (Pending, Running, Succeeded, Completed, Failed)
- groupversion_info.go: SchemeBuilder, AddToScheme

## Types

```go
type AgentRunSpec struct {
    IssueID    string `json:"issueId"`
    Repo       string `json:"repo"`
    BaseBranch string `json:"baseBranch"`
    Model      string `json:"model"`
    Workstream string `json:"workstream"`
    TimeoutSec int    `json:"timeoutSec,omitempty"`
}

type AgentRunStatus struct {
    Phase        string             `json:"phase"`
    Conditions   []metav1.Condition `json:"conditions,omitempty"`
    WorkerTask   string             `json:"workerTask,omitempty"`
    ReviewerTask string             `json:"reviewerTask,omitempty"`
    PrURL        string             `json:"prUrl,omitempty"`
    LastError    string             `json:"lastError,omitempty"`
}
```
