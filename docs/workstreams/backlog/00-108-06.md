---
ws_id: 00-108-06
feature_id: F108
status: backlog
priority: P0
size: M
depends_on: []
---

# 00-108-06: Discovery → Delivery — контракт перехода

Feature: F108

## Goal

На GO-вердикте `cmd/sdp/cmd_discover.go` создаёт `control.FeatureCard` (поле `DiscoveryDir` = путь к артефактам) и заглушку воркстрима `docs/workstreams/backlog/00-NEW-01.md`. Delivery-агент читает `DiscoveryDir` из карточки — не ищет по slug.

## Beads

- sdplab-qdud

## Scope Files

- `internal/control/control.go` — добавить поле `DiscoveryDir string` в `FeatureCard`
- `cmd/sdp/cmd_discover.go` — добавить `createFeatureCard()` и `createWorkstreamStub()`; вызвать при `VerdictGO`
- `cmd/sdp/cmd_discover_test.go` — создать (или добавить к существующему)

## Acceptance Criteria

- [ ] `FeatureCard.DiscoveryDir` — поле добавлено в struct и в YAML/JSON tags
- [ ] `createFeatureCard(store, slug, frame, hyp, discoveryDir)` создаёт карточку с:
  - `NormalizedIntent = frame.ProblemStatement`
  - `DiscoveryDir = discoveryDir` (абсолютный путь к `docs/discovery/<slug>/`)
  - `Status = "shaping"`
  - `AcceptanceShape = hyp.Requirements` (если hyp != nil)
- [ ] `createWorkstreamStub(wsDir, wsID, featureID, frame, hyp, discoveryDir)` создаёт MD-файл с YAML frontmatter + Goal + AC + discovery_dir ссылкой
- [ ] На GO-вердикте оба вызова выполняются; ошибки — warning (не fatal)
- [ ] `go test ./cmd/sdp/... -run TestCreateFeatureCard -race` PASS
- [ ] `go test ./cmd/sdp/... -run TestCreateWorkstreamStub -race` PASS
- [ ] `go build ./cmd/sdp/` успешен

## Out of Scope

- Автоматическое назначение финального wsID/featureID (оператор переименовывает `00-NEW-01` после назначения номера)
- Изменение Beads issue creation (уже работает)
- Delivery-агент (чтение DiscoveryDir — это задача Delivery)

## Implementation Notes

Подробные шаги — в `docs/plans/2026-04-11-f108-architecture-normalization-plan.md`, раздел WS-06.

`control.NewStore(projectRoot)` — конструктор стора. `projectRoot` = `filepath.Dir(filepath.Dir(absOut))` где `absOut = docs/discovery/slug/`.

Воркстрим-стаб использует `wsID = "00-NEW-01"` как placeholder — оператор переименовывает файл при назначении реального feature number.

`frame.Scope` — строка "what's in, what's out" из Phase 1.
`hyp.Requirements` — `[]string` из Phase 2.
