---
ws_id: 00-137-03
feature_id: F137
status: done
priority: P1
size: M
depends_on:
  - 00-137-02
---

# 00-137-03: Command migration onto registry

Feature: F137 (CLI Surface Normalization)

## Goal

Move the initial high-value command set onto the registry without changing product behavior, so downstream callers can depend on one canonical surface.

## Beads

- sdplab-pg7o.3

## Scope Files

- `cmd/sdp/main.go` — route through registry-backed command execution
- `cmd/sdp/cmd_scout.go` — registry migration candidate
- `cmd/sdp/cmd_architect.go` — registry migration candidate
- `cmd/sdp/cmd_metrics.go` — registry migration candidate
- `cmd/sdp/cmd_dispatch.go` — registry migration candidate
- `cmd/sdp/cmd_orchestrate.go` — registry migration candidate

## Acceptance Criteria

- [ ] The selected high-value commands execute through the registry path
- [ ] Existing behavior and flag semantics remain stable for migrated commands
- [ ] The migration set is sufficient for mini-harness and sweep dependency planning
- [ ] Tests cover migrated command registration and execution dispatch

## Out of Scope

- Rewriting command business logic for new product behavior
- Migrating every historical subcommand in one pass
