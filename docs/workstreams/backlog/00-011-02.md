# WS-011-02: WorkspaceManager fork support + multi-remote

Feature: FR-011
Priority: P0
Effort: 1d
Dependencies: WS-011-01

## Goal

Extend WorkspaceManager to handle fork-based repos (origin + upstream remotes), branch tracking, and cross-project workspace isolation.

## Acceptance Criteria

- [x] EnsureWorkspace clones with correct remote
- [x] Fork repos: origin = fork, upstream = source
- [x] git fetch upstream works for fork repos
- [x] Workspace paths isolated per project
- [x] Unit tests with mock git
