# WS-011-04: Federation bridge for all registered projects

Feature: FR-011
Priority: P1
Effort: 1d
Dependencies: WS-011-03

## Goal

Ensure federation bridge spins up per registered project and routes intake/ready events correctly.

## Acceptance Criteria

- [x] Bridge starts for each project in registry (feature-orchestrator, swarm-orchestrator)
- [x] Beads adapter uses correct workdir per project (EnsureWorkspaceFromProject)
- [x] NATS subjects use project-specific prefixes (sdp.intake.{projectID}, sdp.beads.{projectID}.ready)
- [x] Cross-project dependency tracking via Beads dep add
- [x] Integration test: intake for 2 different projects (TestIntakeTwoProjects)

## Execution Report (2026-02-22)

- cmd/feature-orchestrator: loop store.List(), start Bridge per project with per-project workspace
- cmd/swarm-orchestrator: same Bridge startup
- internal/federation/intake_integration_test: TestIntakeTwoProjects

## Execution Report (2026-02-22) — Cross-Project Deps

**Wiring:** Bridge handleIntake already calls `adapter.DepAdd(id, blockerID)` for each `req.DependsOn`. Intake payload supports `depends_on: ["issueID", "projectID:issueID"]`. Cross-project format (projectID:issueID) is passed to DepAdd; DepsClosed resolution for cross-project blockers requires multi-project adapter lookup (follow-up).
