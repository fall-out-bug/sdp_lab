---
ws_id: 00-053-40
feature_id: F053
status: done
priority: P2
size: S
depends_on: []
---

# 00-053-40: Coverage Files in Root — Gitignore

Feature: F053 (sdp_dev-go6a)  
Bead: sdp_dev-yi2i (review finding)

## Goal

Ensure coverage files (coverage.out, coverage.html, etc.) are gitignored and not committed. CI may write to root; local runs should not pollute repo.

## Scope Files

- `.gitignore`
- `Makefile` (if coverage target writes to root)
- `.github/workflows/*.yml` (if coverage artifact path)

## Dependencies

None.

## Acceptance Criteria

- [x] coverage.out, coverage.html, *.cover, cover.out in .gitignore
- [x] No coverage.* files tracked in git
- [x] CI coverage job writes to predictable path (e.g. build/coverage.out) or artifact dir
- [x] go test -coverprofile=... does not leave untracked files in root (or they are ignored)

---

## Execution Report

**Completed:** 2026-02-25

**Deliverables:**
- .gitignore: added coverage.html, *.cover, cover.out (coverage.out, *.out already present)
- Verified no coverage.* files tracked in git
- CI: added coverage step writing to .tmp/coverage.out, upload as artifact (retention 7 days)
- Makefile (00-053-39): coverage already writes to .tmp/ — root stays clean
- go build ./... passes

**Existing work summary:** 00-053-39 moved coverage to .tmp/. Extended .gitignore and added CI coverage artifact.

## Beads

- sdplab-go6a: legacy reference (done)
