---
ws_id: 00-134-06
feature_id: F134
status: open
priority: P1
size: M
depends_on: [00-134-01]
---

# 00-134-06: Skill integration — hybrid mode (F125 intents + strict mode phase invocation)

Feature: F134 (sdplab-i01)
Beads: sdplab-ob7

## Goal

Update existing F125 intent skills to optionally invoke phase commands in strict mode. Fix weak autonomy in build.md.

## Scope Files

- `.agents/skills/build.md` — add strict-mode invocation + provenance pattern
- `.agents/skills/understand.md` — add optional plan phase step
- `.agents/skills/review.md` — add evidence enforcement invocation
- `.agents/skills/operate.md` — add deploy gate pre-flight check

## Work

On build.md:
- Add MUST DO entry: if `--strict-mode` or `SDP_STRICT=true`, invoke `sdp plan` before implementation, `sdp eval` after
- Add provenance-commit pattern (F134-04 trailer) to MUST DO list
- Fix weak autonomy per sdplab-3b5 (add MUST DO / MUST NOT DO / Response Format)

On understand.md:
- Add optional step: invoke `sdp plan` for design phase artifacts

On review.md:
- MUST DO: invoke `sdp review`; verify evidence.json present via F134-03

On operate.md:
- Deploy mode blocked without all phase gates passed; add pre-flight check

## Acceptance Criteria

- [ ] All 4 skill files updated with strict-mode / phase invocation sections
- [ ] build.md has MUST DO / MUST NOT DO / Response Format sections
- [ ] `sdp-protocol-check --lint-skills` green (if available)
- [ ] No skill file exceeds 120 lines

## Out of Scope

- New phase-first skills (F134-07)
- Changing F125 intent names
