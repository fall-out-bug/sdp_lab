---
ws_id: 00-108-05
feature_id: F108
status: backlog
priority: P0
size: S
depends_on: ["00-108-03", "00-108-04"]
---

# 00-108-05: Wire `sdp-harness` с LiveGateway + интеграционный тест

Feature: F108

## Goal

Заменить `agentloop.NewStubGateway()` в `cmd/sdp-harness/main.go` на `livegw.New()`. Пустой `OPENROUTER_API_KEY` — явная ошибка, не тихий fallback. Добавить интеграционные тесты.

## Beads

- sdplab-8ij

## Scope Files

- `cmd/sdp-harness/main.go` — заменить `NewStubGateway()` на `livegw.New(os.Getenv("OPENROUTER_API_KEY"), "")`
- `cmd/sdp-harness/main_integration_test.go` — создать

## Acceptance Criteria

- [ ] `cmd/sdp-harness/main.go` не содержит `NewStubGateway` (кроме тестов)
- [ ] `cmdRun` читает `OPENROUTER_API_KEY` из env; если пустой — возвращает ошибку с упоминанием "OPENROUTER_API_KEY" или "API key"
- [ ] `TestCmdNew_createSession` — новая сессия создаётся, DB-файл существует
- [ ] `TestCmdRun_noAPIKey` — пустой ключ → ошибка (не panic, не silent stub)
- [ ] `TestCmdRun_terminatedSession` — `errors.Is(err, agentloop.ErrHarnessTerminated)` returns true
- [ ] `go test ./cmd/sdp-harness/... -race` PASS
- [ ] `go build ./cmd/sdp-harness/` — бинарь компилируется

## Out of Scope

- End-to-end тест с реальным OpenRouter API (нет в CI)
- Изменение `new` subcommand

## Implementation Notes

Подробные шаги — в `docs/plans/2026-04-11-f108-architecture-normalization-plan.md`, раздел WS-05.

Замена в `cmdRun()` (строки ~160-162):
```go
// Было:
gateway := agentloop.NewStubGateway()

// Стало:
apiKey := os.Getenv("OPENROUTER_API_KEY")
gw, err := livegw.New(apiKey, "")
if err != nil {
    return fmt.Errorf("create LiveGateway: %w\n(hint: set OPENROUTER_API_KEY env var)", err)
}
```

Добавить import `"sdp_dev/internal/agentloop/livegw"`.
