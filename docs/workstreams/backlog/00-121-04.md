---
ws_id: 00-121-04
feature_id: F121
status: done
priority: P2
size: M
depends_on:
  - 00-121-03
---

# 00-121-04: Code Quality — Stdlib Migration and Bug Fixes

Feature: F121 (Toolkit Metrics — git-derived process health)

## Goal

Fix 6 code-quality findings from the F121 review: replace custom string helpers with stdlib, deduplicate insertion sort, add missing threshold rater, fix wrong rater calls in CLI, guard Period zero-value rendering, and remove empty import.

## Beads

- (no beads created — pre-beads workstream)

## Scope Files

- `internal/metrics/types.go`
- `internal/metrics/helpers.go`
- `internal/metrics/hygiene.go`
- `internal/metrics/thresholds.go`
- `internal/metrics/gitflow.go`
- `internal/metrics/waste.go`
- `internal/metrics/knowledge.go`
- `cmd/sdp/cmd_metrics.go`

## Review Findings Addressed

| # | Severity | Source | Finding |
|---|----------|--------|---------|
| 1 | P2 | techlead | Custom string helpers vs stdlib |
| 2 | P2 | techlead | Insertion sort duplicated 3x |
| 3 | P2 | techlead | Wrong rater for conventional commits in renderText |
| 4 | P2 | techlead | Period zero-value rendered as 0001-01-02 |
| 5 | P3 | techlead | Empty import () in hygiene.go |
| 6 | P2 | docs | Missing godoc on 5 analyzer functions |

## Acceptance Criteria

- [x] Custom helpers (toLower, contains, stringContains, hasSuffix, hasPrefix, indexOfChar, imin, imax) replaced with stdlib `strings` and `slices` packages where equivalent
- [x] Insertion sort replaced with `slices.SortFunc` using `cmp.Compare`; gitflow.go, waste.go, knowledge.go call it instead of duplicating
- [x] `RateConventionalCommitsRatio` added to thresholds.go with thresholds: green >0.7, yellow 0.4-0.7, red <0.4
- [x] cmd_metrics.go lines ~99 and ~149 use `RateConventionalCommitsRatio` instead of `RateTicketLinkedRatio` for conventional commits ratio
- [x] renderText and renderMarkdown guard against Period zero-value (display "N/A" or skip when From.IsZero())
- [x] Empty `import ()` removed from hygiene.go
- [x] go build ./... passes
- [x] go test ./internal/metrics/... ./cmd/sdp/... passes
