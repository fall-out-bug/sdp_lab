---
ws_id: 00-106-06
feature_id: F106
status: open
priority: P1
size: XS
depends_on: []
---

# 00-106-06: Pipeline doc — заморозить Status и классифицировать gates

Feature: F106 (agentloop Integration)

## Goal

Обновить `docs/CANONICAL_SDP_PIPELINE.md`: изменить Status с "Proposed" на "Active v1.0", добавить явную классификацию gates (mandatory/advisory) и уточнить что enforcement gates работают только после F106.

## Beads

- sdplab-84v

## Scope Files

- `docs/CANONICAL_SDP_PIPELINE.md` — модифицировать

## Acceptance Criteria

- [ ] Status изменён с `Proposed` на `Active v1.0` с датой 2026-04-11
- [ ] Добавлена секция "Gate Enforcement Status" с таблицей mandatory/advisory для каждого gate
- [ ] В таблице явно указано: `review-pass` и `qa-pass` становятся mandatory enforcement после F106 (до — advisory)
- [ ] Добавлено примечание: "Pipeline is law as of v1.0. Amendments require explicit owner sign-off."
- [ ] `sdp-doc-sync --mode check --strict` PASS (нет broken links в изменённом файле)

## Out of Scope

- Изменение содержимого фаз или gates — только статус и классификация
- Другие документы

## Implementation Notes

Добавить секцию после Gate Model:

```markdown
## 4a. Gate Enforcement Status

| Gate | Type | Enforcement |
|------|------|-------------|
| `contract-approve` | human | **Mandatory** — pipeline blocked without approval |
| `scope-review` | human | **Mandatory** — if out-of-scope detected |
| `review-pass` | automated | **Mandatory after F106** — GateEngine evaluates from tool evidence; Advisory before |
| `qa-pass` | automated | **Mandatory after F106** — GateEngine evaluates from tool evidence; Advisory before |
| `ci` | automated | **Mandatory** — CI pipeline enforces |
| `staging-approve` | human | **Mandatory** |
| `prod-approve` | human | **Mandatory** |

> Prior to F106 (agentloop integration), automated gates (`review-pass`, `qa-pass`) 
> evaluate evidence from stdout parsing only — advisory status.
> After F106, these gates evaluate from real tool call evidence — mandatory status.
```
