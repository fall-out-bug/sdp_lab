---
ws_id: 00-053-34
feature_id: F053
status: completed
priority: P2
size: L
depends_on: []
---

# 00-053-34: TechLead — exec Coupling + LLM Invoker

Feature: F053 (sdp_dev-h87o)

## Goal

Abstract exec.Command in orchestrate/evidence/guard; LLM invoker interface.

## Beads

- sdp_dev-4736: orchestrate/evidence/guard use direct exec.Command — no abstraction
- sdp_dev-p7xv: No LLM invoker interface — InvokeOpenCode hardcoded, RunBuildPhase untested

## Scope Files

- `internal/orchestrate/attest.go`, `advance.go`, `hydrate_sources.go`, `policy.go`
- `internal/evidence/auto_attest.go`
- `internal/guard/scope_check.go`
- `internal/orchestrate/invoke_opencode.go`

## Acceptance Criteria

- [x] Introduce CommandRunner or equivalent for orchestrate/evidence/guard
- [x] InvokeOpenCode behind interface; tests with fake
- [x] RunBuildPhase has unit tests
- [x] go build ./... passes
- [x] go test ./... passes

---

## Execution Report (2026-02-25)

**Verdict:** PASS

1. **CommandRunner:** Added `internal/executil/runner.go` with `CommandRunner` interface (Output, CombinedOutput, Run) and `DefaultRunner`. Refactored orchestrate (attest, advance, hydrate_sources, policy), evidence (auto_attest), guard (scope_check) to use `executil.DefaultRunner` instead of direct `exec.Command`.
2. **LLMInvoker:** Added `internal/orchestrate/llm.go` with `LLMInvoker` interface and `DefaultLLMInvoker` (wraps InvokeOpenCode). `RunBuildPhase` and `RunReviewPhase` accept optional `invoker` param; nil uses default.
3. **RunBuildPhase tests:** Added `TestRunBuildPhase_WithFakeInvoker` and `TestRunBuildPhase_WithFakeInvoker_NonZeroExit` with `fakeLLMInvoker` in `invoke_opencode_test.go`.
4. **executil test:** Added `runner_test.go` for DefaultRunner Output/Run.
5. **go build ./...** and **go vet ./...** pass.
