# WS-017-02: OpenTelemetry SDK integration for all components

Feature: FR-017
Priority: P0
Effort: 1.5d
Dependencies: none

## Goal

Add go.opentelemetry.io/otel to go.mod. Instrument adapter-controller, swarm-worker, swarm-reviewer, orchestrator with OTLP trace export.

## Acceptance Criteria

- [x] go.opentelemetry.io/otel, otlptracehttp added to go.mod
- [x] setupTracing() helper in internal/observability/
- [x] adapter-controller: reconcile loop spans
- [x] swarm-worker: execute/verify/publish spans
- [x] swarm-reviewer: review spans
- [x] W3C tracecontext propagation via NATS headers (PublishWithContext, SubscribeWithContext)
- [x] Existing JSONL emission preserved (dual-write)
- [x] OTEL Collector receives traces (when OTEL_EXPORTER_OTLP_ENDPOINT set)

## Review Results (2026-02-22)

- **QA:** PASS — tracing_test.go covers SetupTracing disabled path
- **Security:** PASS — OTLP over HTTP, no secrets in spans
- **TechLead:** PASS — SetupTracing in observability, spans in key flows
- **DevOps:** Bead sdp_dev-twa — W3C propagation via NATS (deferred)
- **Verdict:** APPROVED (W3C propagation tracked in bead)

## Execution Report (2026-02-22)

**W3C tracecontext (sdp_dev-twa) implemented:**
- `internal/bus/trace_propagation.go` — natsHeaderCarrier, inject/extract
- `Bus.PublishWithContext(ctx, subject, envelope)` — injects traceparent/tracestate
- `Bus.SubscribeWithContext(subject, queue, handler func(context.Context, Envelope))` — extracts and runs in context
- `swarm.Dispatcher.DispatchWithContext(ctx, task, role)` — propagates trace
- `cmd/swarm-orchestrator` — uses SubscribeWithContext + DispatchWithContext
- Tests: `TestPublishWithContext_InjectTraceHeaders`, `TestSubscribeWithContext_ExtractTraceContext`
