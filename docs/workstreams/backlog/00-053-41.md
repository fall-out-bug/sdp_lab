---
ws_id: 00-053-41
feature_id: F053
status: done
priority: P2
size: M
depends_on: []
---

# 00-053-41: Unified Logging Strategy

Feature: F053 (sdp_dev-hcte)  
Bead: sdp_dev-ftrq (review finding)

## Goal

Define and document unified logging strategy. Evidence, executor, verifier, orchestrate use slog inconsistently. Establish conventions: level (Info/Warn/Error), structured fields, and when to log vs return errors.

## Scope Files

- `internal/evidence/`
- `internal/orchestrate/`
- `internal/ciloop/`
- `sdp/sdp-plugin/internal/`
- `docs/decisions/` (new DECISION if needed)

## Dependencies

None.

## Acceptance Criteria

- [x] Document logging conventions (slog levels, key names, when to use Debug vs Info)
- [x] At least one package migrated to conventions as reference
- [x] No fmt.Print/log.Print in production paths
- [x] go build ./... passes
- [x] go test ./... passes

---

## Execution Report

**Completed:** 2026-02-25

**Deliverables:**
- docs/decisions/ADR-003-logging-strategy.md: slog levels, structured field names (error, ws_id, feature, phase, msg), when to log vs return, CLI vs internal
- internal/orchestrate/loop.go: migrated to ws_id, phase; added package doc referencing ADR-003
- Verified: no log.Print in internal/; fmt.Println in loop.go is orchestration marker (CI GREEN) — acceptable per ADR
- go build ./... passes

**Existing work summary:** orchestrate, ciloop, sdp-plugin used slog with inconsistent keys. ADR-003 establishes conventions; orchestrate migrated as reference.

## Beads

- sdplab-hcte: legacy reference (done)
