---
ws_id: 00-053-10
feature_id: F053
status: done
priority: P2
size: M
depends_on: []
---

# 00-053-10: Executor ParseDependencies + Context Cancellation

Feature: F053 (sdp_dev-i8l1)

## Goal

Fix executor: ParseDependencies error path should not continue with wrong order; add mid-execution ctx cancellation check. Addresses sdp_dev-6h50, yedi.

## Beads

- Historical feature bead: `sdp_dev-i8l1`
- sdplab-ybf: Bucket C1 tracker for legacy `## Beads` normalization in `00-053-01` ... `00-053-10`

## Scope Files

- `sdp/sdp-plugin/internal/executor/execution.go`
- `sdp/sdp-plugin/internal/executor/execution_test.go`
- `sdp`
- `docs/workstreams/backlog/00-053-10.md`
- `docs/workstreams/INDEX.md`
- `.beads-sdp-mapping.jsonl`

## Dependencies

None.

## Acceptance Criteria

- [x] ParseDependencies error: fail or use safe fallback, don't continue with empty deps
- [x] Long-running workstreams check ctx.Done() periodically
- [x] Tests for both scenarios
- [x] go test ./internal/executor/... passes

## Execution Report

- **ParseDependencies**: On error, use safe fallback `dependencies[wsID] = []string{}` instead of skipping; warning still logged.
- **Context cancellation**: Propagate `context.Canceled`/`DeadlineExceeded` from runner to caller; added `blockingRunner` and `TestExecutor_ContextCancellationMidExecution`.
- **Tests**: `TestExecutor_ParseDependenciesError_SafeFallback`, `TestExecutor_ContextCancellationMidExecution`.

## Out of Scope

- Changing dependency format
- Retry logic
