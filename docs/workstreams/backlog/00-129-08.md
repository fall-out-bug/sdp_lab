---
ws_id: 00-129-08
feature_id: F129
status: open
priority: P1
size: S
depends_on: []
---

# 00-129-08: Architect tests in CI

Feature: F129 (Autonomous Operations & Regression Hardening)

## Goal

Wire `tests/architect/*` into the CI pipeline — currently runs only locally. Part of the regression safety net.

## Beads

- sdplab-8k5.8

## Scope Files

- `.github/workflows/ci.yml` — add `architect-tests` job
- `tests/architect/README.md` — document the runner contract
- `Makefile` or `scripts/run_architect_tests.sh` — CI entry-point

## Acceptance Criteria

- [ ] New CI job runs `tests/architect/*` on every PR
- [ ] Failures block merge
- [ ] Runtime ≤5 minutes (subsample if needed)
- [ ] Test output published as a CI artifact

## Out of Scope

- Expanding the test corpus
- Architect engine changes

## Implementation Notes

Investigate why `tests/architect` is not currently in CI — may require an LLM API key that only exists in main-branch secrets. If so: run only post-merge, not on PRs from forks.
