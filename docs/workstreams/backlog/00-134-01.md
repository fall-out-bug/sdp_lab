---
ws_id: 00-134-01
feature_id: F134
status: open
priority: P1
size: M
depends_on: []
---

# 00-134-01: Phase commands — sdp plan / sdp review / sdp eval CLI wiring

Feature: F134 (sdplab-i01)
Beads: sdplab-kkk

## Goal

Wire `sdp plan`, `sdp review`, `sdp eval` as first-class CLI entry points over existing `internal/discovery/` and `internal/gate/`.

## Scope Files

- `cmd/sdp/cmd_plan.go` — NEW: Plan phase command
- `cmd/sdp/cmd_review.go` — NEW: Review phase command
- `cmd/sdp/cmd_eval.go` — NEW: Eval phase command
- `cmd/sdp/main.go` — wire new commands

## Existing Code to Reuse

- `internal/discovery/` — 10 files, full phase FSM
- `internal/gate/gate.go` — Gate struct with Question/Options/Answer/Resolved
- `internal/gate/beads.go` — beads integration
- `internal/evidence/` — 20 files, attestation + trace_validator

## Acceptance Criteria

- [x] `sdp phase plan --feature-id F` works — creates delta + gate, persists files
- [x] `sdp phase review --feature-id F` works — same pattern
- [x] `sdp phase eval --feature-id F` works — same pattern
- [x] `--evidence-path` flag for evidence validation (required in strict mode)
- [x] Non-strict mode auto-approves (warns without evidence); strict mode validates evidence and blocks for human
- [x] Delta + trace persisted to `.sdp/phases/<run_id>/`
- [x] Unit tests for flag parsing, validation, gate creation, delta creation
- [x] `go vet` clean

## Follow-up Work

- Context loading from beads issues (read WS files, feature description)
- Gate persistence via beads (`bd human` integration)
- Strict mode gate waiting (poll beads for resolution, `sdp approve` integration)
- Trace record emission

## Out of Scope

- Delta artifact format (F134-02)
- Evidence enforcement (F134-03)
- Skill integration (F134-06)
