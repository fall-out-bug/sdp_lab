---
ws_id: 00-137-05
feature_id: F137
status: open
priority: P1
size: S
depends_on:
  - 00-137-03
  - 00-137-04
---

# 00-137-05: CLI reference + parity gate

Feature: F137 (CLI Surface Normalization)

## Goal

Make CLI docs and doc lint reflect the registry truth so command drift becomes detectable instead of anecdotal.

## Beads

- sdplab-pg7o.5

## Scope Files

- `docs/reference/sdp-cli.md` — canonical CLI reference
- `internal/docsync/` — parity check for registry vs documented commands
- `cmd/sdp-doc-sync/main.go` — expose the new parity rule in the existing doc-sync surface

## Acceptance Criteria

- [ ] `docs/reference/sdp-cli.md` exists and matches the registry surface in scope
- [ ] Doc lint can detect commands present in the registry but missing from the CLI reference
- [ ] Legacy shim/deprecation state is reflected in CLI docs
- [ ] The parity rule is automated rather than enforced manually

## Out of Scope

- Skill catalog docs
- MCP docs
