---
ws_id: 00-053-32
feature_id: F053
status: done
priority: P2
size: S
depends_on: []
---

# 00-053-32: Evidence Event + Emit Errors

Feature: F053 (sdp_dev-nsmo)

## Goal

Evidence Event schema validation, Emit errors returned.

## Beads

- sdp_dev-s97b: evidence Event validation only ID/Type/Timestamp — no schema
- sdp_dev-c8rm: Emit() errors logged but not returned — silent failures

## Scope Files

- `sdp/sdp-plugin/internal/evidence/emitter.go`
- `sdp/sdp-plugin/internal/evidence/` (Event struct, validation)

## Acceptance Criteria

- [x] Event validation against schema or extended struct
- [x] Emit returns error to caller (or documented why async-only)
- [x] go build ./... passes
- [x] go test ./internal/evidence/... passes

---

## Execution Report

**Date:** 2026-02-25  
**Verdict:** PASS

- **Event validation:** Added ValidateEvent() checking ID, Type, Timestamp; emitSync returns error instead of silently skipping.
- **Emit returns error:** Emit now validates synchronously and returns validation error to caller; write errors remain async (logged).
