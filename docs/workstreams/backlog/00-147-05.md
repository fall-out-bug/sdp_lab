---
ws_id: 00-147-05
feature_id: F147
status: open
priority: P1
size: S
depends_on: ["00-147-03"]
---

# 00-147-05: `BdTypeMicro` — embedding-based bd type classifier

Feature: F147 (MicroFirst Inference Tier)

Design reference: [2026-04-27-f147-microfirst-tier-design.md §3 F147-05](../../plans/2026-04-27-f147-microfirst-tier-design.md)

## Goal

Embedding-based k-NN classifier для bd issue type (`bug/task/feature`). Параллелен BdSeverityMicro (00-147-04), переиспользует embedding/k-NN infra (00-147-03). `chore` маппится в `task`, `epic` исключается из train.

## Beads

- sdplab-rp3u.5 (historical leaf issue)

## Scope Files

- `internal/inference/microfirst/bdtype/classifier.go` — new. `BdTypeMicro` Stage[BdInput, BdTypeResult].
- `internal/inference/microfirst/bdtype/corpus.go` — new. Loader, фильтрация (closed; type ∈ {bug, task, feature, chore}; chore→task; epic excluded).
- `internal/inference/microfirst/bdtype/result.go` — new. `BdTypeResult{Type, Confidence, Status, Neighbors}` implements `Confider`.
- `internal/inference/microfirst/bdtype/classifier_test.go` — new.
- `internal/inference/microfirst/bdtype/testdata/sample_issues.jsonl` — new.

## Acceptance Criteria

- [ ] Loader: тот же формат что 00-147-04, фильтр по type.
- [ ] `chore` → нормализуется в `task` при загрузке корпуса.
- [ ] `epic` → исключается из train.
- [ ] Threshold: top-1 cosine ≥ 0.80 AND top-3 unanimous → `Status=OK`.
- [ ] Eval accuracy on `Status=OK` ≥ 85% (3-class легче 4-class из 00-147-04).
- [ ] Fallback rate ≤ 40%.
- [ ] Latency p95 < 100ms.
- [ ] Coverage ≥ 85%.
- [ ] `go build ./...` and `go vet ./...` clean.

## Out of Scope

- CLI wrapper `bd suggest` (→ 00-147-08).
- Severity classifier (→ 00-147-04, parallel WS).
