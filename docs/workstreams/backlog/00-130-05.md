---
ws_id: 00-130-05
feature_id: F130
status: open
priority: P2
size: M
depends_on: [00-130-04]
---

# 00-130-05: Bootstrap Integration — sdp bootstrap + sdp rules update

Feature: F130 (sdplab-9nb)

## Goal

Wire full pipeline: scout.Report → conventions → rules → adapter → harness config. Add `sdp rules update` subcommand for mature repos.

## Beads

- sdplab-11i

## Scope Files

- `cmd/sdp/cmd_bootstrap.go` — extend: wire scout→adapter pipeline
- `cmd/sdp/cmd_rules.go` — new: `sdp rules update` subcommand
- `cmd/sdp/cmd_rules_test.go` — new: tests

## Acceptance Criteria

- [ ] `sdp bootstrap` runs scout → conventions extraction → adapter → writes DRAFT config files (F130-09 contract)
- [ ] `sdp rules update` refreshes existing rules from new evidence
- [ ] Three lifecycle modes: greenfield, brownfield-new, brownfield-mature
- [ ] DRAFT files are NEVER auto-committed (F130-09 contract enforced)
- [ ] Integration test: full pipeline on fixture project produces valid CLAUDE.md section

## Out of Scope

- Interactive mode (F130-06)
- Delta analysis (F130-07)

## Implementation Notes

- Check existing `cmd/sdp/cmd_bootstrap.go` before writing — may have partial implementation
- Wire: scout.New() → scout.Run() → conventions → rules.Generate() → adapter.Render()
- `sdp rules update` is a standalone command for refreshing rules in mature repos
