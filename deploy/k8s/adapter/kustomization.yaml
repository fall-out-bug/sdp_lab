# Root adapter kustomization: defaults to dev overlay (emptyDir, E2E).
# Use overlays/staging or overlays/prod for PVC-backed deployments.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - overlays/dev
