---
apiVersion: v1
kind: ConfigMap
metadata:
  name: cicd-config
  namespace: sdp-control
data:
  target_env: "dev"
  registry: "ghcr.io/fall-out-bug"
  # SSH access: set DEPLOY_SSH_HOST env or add to secret cicd-deploy-config
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cicd-agent
  namespace: sdp-control
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cicd-agent
  template:
    metadata:
      labels:
        app: cicd-agent
    spec:
      containers:
        - name: agent
          image: sdp/cicd-agent:latest
          env:
            - name: NATS_URL
              value: "nats://nats:4222"
            - name: GHCR_REGISTRY
              value: "ghcr.io/fall-out-bug"
            - name: CICD_WORK_DIR
              value: "/workspace"
            - name: KUBECONFIG
              value: "/etc/deploy/kubeconfig"
            - name: CICD_IMAGES
              value: "adapter-controller,feature-orchestrator,swarm-orchestrator,intake-gateway,registry-agent,telemetry-analyzer,cicd-agent"
          volumeMounts:
            - name: workspace
              mountPath: /workspace
            - name: docker-socket
              mountPath: /var/run/docker.sock
            - name: deploy-config
              mountPath: /etc/deploy
              readOnly: true
      volumes:
        - name: workspace
          emptyDir: {}
        - name: docker-socket
          hostPath:
            path: /var/run/docker.sock
            type: Socket
        - name: deploy-config
          secret:
            secretName: cicd-deploy-config
            optional: true
