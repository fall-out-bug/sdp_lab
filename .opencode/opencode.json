{
  "mcpServers": {},
  "defaultAgent": "orchestrator",
  "_comment": "For oneshot: route feature commands to sdp-orchestrate as outer loop. agent: orchestrator maps to sdp-orchestrate invocation. opencode lacks Stop hooks — outer loop CLI replaces them."
}
