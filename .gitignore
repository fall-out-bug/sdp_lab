.sdp/*
!.sdp/config.yml
!.sdp/pipeline-hooks.yaml.example
!.sdp/policies/
!.sdp/agent-constraints.yaml
!.sdp/metrics/
!.sdp/metrics/coverage.txt

# opencode-mem storage
.sdp/mem/

# session-only wisps (ephemeral work items)
.sdp/session/
__pycache__/
*.pyc
.tmp/
.kube/

# Coverage files
*.out

# Go binaries (built locally, root only)
/adapter-controller
/autonomy-worker
/feature-orchestrator
/intake-gateway
/orchestrator
/pr-publish
/registry-agent
/swarm-orchestrator
/swarm-worker
/telemetry-analyzer
/sdp-evidence
/sdp-ci-loop
/sdp-orchestrate
/sdp-eval
/sdp-guard
/beads-fsm
/brain-gateway
/cicd-agent

# Build output (make build-sdp-*)
bin/

# Execution state
.oneshot/

# SDP harness config (F128: sdp/ submodule retired, F128-06: tracked symlinks → prompts/)
# Tracked symlinks: .claude/agents → ../prompts/agents, .claude/skills → ../.agents/skills
# sdp/ is a local live clone of github.com/fall-out-bug/sdp for scripts/sdp-publish.sh (F128-07)
sdp/
.claude/worktrees/
.cursor/skills
.cursor/agents
.opencode/skills
.prompts
evaluator-orchestrator
sdp-ready

.cache/
.sisyphus/
/sdp-gh-findings-sync
sdp-up

# Dolt database files (added by bd init)
.dolt/
*.db
sdp-dispatch

# Env files (secrets)
.env
.env.*

# Cursor hooks state
.cursor/hooks/

# Stale git artifacts
ORIG_HEAD

# Additional build artifacts
sdp_bin
sdp-beads-bridge

# sdp/ — optional local checkout of public sdp repo (https://github.com/fall-out-bug/sdp)
# Protocol artifacts live at native paths: prompts/, schema/, templates/, .claude/hooks/
/sdp

# Git worktrees
.worktrees/

# Local RTK config
.rtk/
sdp-doc-sync
sdp-doc-sync

# MCP server binary (rebuilt from source)
sdp-mcp
*.test
